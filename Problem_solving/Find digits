function findDigits(n) {
    // Write your code here
    let temp = n, count =0;
    while(n > 0){
        let rem = n % 10;
        if(temp % rem === 0){
            count++;
        }
        n = Math.floor(n/10)
    }
    return count;
}
